====== Global Report for Rasca Backups ======

**DO NOT EDIT THIS PAGE. It is generated by modularit-inventory chef cookbook. Any changes will be overwriten on next run**

==== Totals =====

^ Alert ^ OK ^ WARNING ^ CRITICAL ^ UNKNOWN ^ RECOVERED ^
^ CheckDuplicity | <%= @totals["CheckDuplicity"]["OK"] %> | <%= @totals["CheckDuplicity"]["WARNING"] %> | <%= @totals["CheckDuplicity"]["CRITICAL"] %> | <%= @totals["CheckDuplicity"]["UNKNOWN"] %> | <%= @totals["CheckDuplicity"]["RECOVERED"] %> |
^ CheckBackup ^ <%= @totals["CheckBackup"]["OK"] %> | <%= @totals["CheckBackup"]["WARNING"] %> | <%= @totals["CheckBackup"]["CRITICAL"] %> | <%= @totals["CheckBackup"]["UNKNOWN"] %> | <%= @totals["CheckBackup"]["RECOVERED"] %> |

<% @admindomains.each do |d| -%>

==== Backups for <%= d.name %> =====

{{page><%= d.name %>:rasca_reports:backup#CheckSafekeep&noeditbutton}}

{{page><%= d.name %>:rasca_reports:backup#CheckDuplicity&noeditbutton}}

{{page><%= d.name %>:rasca_reports:backup#CheckBackup&noeditbutton}}

<% end -%>
